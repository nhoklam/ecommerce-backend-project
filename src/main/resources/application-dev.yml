server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: nhoklam271 # <-- Mật khẩu của bạn
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # SỬA LỖI: Đổi từ 'update' sang 'create'
      # 'create' sẽ luôn xóa và tạo lại CSDL, hoàn hảo cho việc test seeder
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect 
  
  # ==========================================================
  # == CẤU HÌNH MAILTRAP CHO SPRING BOOT ==
  # ==========================================================
  mail:
    host: smtp.gmail.com
    port: 587 # Cổng cho TLS
    username: "nhoklam2712005@gmail.com" # <--- Email thật của bạn
    password: "reljmhgynkctdguj" # <--- Mật khẩu ứng dụng (xem ghi chú)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Cấu hình file upload (ví dụ)
file:
  upload-dir: "./uploads"

jwt:
  secret: "VGhpc0lzTXlTdXBlckxvbmdBbmRTZWN1cmVTZWNyZXRLZXlGb3JFY29tbWVyY2VQcm9qZWN0MjAyNQ=="
  expiration: 86400000

logging:
  level:
    com.nhom13.ecommerce: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

vnpay:
  tmnCode: "YOUR_SANDBOX_TMNCODE"  # Lấy từ VNPAY
  hashSecret: "YOUR_SANDBOX_HASHSECRET" # Lấy từ VNPAY
  url: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
  returnUrlBase: "http://localhost:8080/api/payments/vnpay_return"
  ipnUrlBase: "http://localhost:8080/api/payments/vnpay_ipn" # Cần public (sử dụng ngrok khi test)