server:
  port: 8080

spring:
  # ===============================================
  # == KÍCH HOẠT PROFILE 'dev' ĐỂ CHẠY SEEDER ==
  # == Thêm 2 dòng này vào file của bạn ==
  profiles:
    active: dev
  # ===============================================

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    # SỬA LỖI: Mật khẩu của bạn đã bị sai định dạng YAML (xuống dòng)
    password: nhoklam271
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # Cảnh báo trong log (HHH90000026) khuyên dùng dialect mới này
        dialect: org.hibernate.dialect.MySQLDialect 

  # ==========================================================
  # == CẤU HÌNH MAILTRAP CHO SPRING BOOT ==
  # ==========================================================
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: "662fe9690ba83b"
    password: "ba28fabb8f56a6"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#... cấu hình jwt và logging của bạn...
jwt:
  secret: "VGhpc0lzTXlTdXBlckxvbmdBbmRTZWN1cmVTZWNyZXRLZXlGb3JFY29tbWVyY2VQcm9qZWN0MjAyNQ=="
  expiration: 86400000

logging:
  level:
    com.nhom13.ecommerce: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG